({
    myAction : function(component, event, helper) {
        helper.fetchAccHelper(component, event, helper);
    },
    showOpp: function(component, event, helper){
        helper.showAllOpp(component, event, helper);
    }
})